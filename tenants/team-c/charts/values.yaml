replicaCount: 2
image:
  repository: nginx
  tag: alpine
service:
  type: ClusterIP
  port: 3000
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 200m
    memory: 350Mi
mongodb:
  image: mongo:7.0
  storageSize: 40Gi
centralDb:
  secretName: central-db-credentials
